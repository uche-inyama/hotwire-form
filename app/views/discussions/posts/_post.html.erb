<div class="my-4">
  <div class="card card-body">
    <h4 class="card-title"><%= post.user.username %></h4>
    <h6 class="card-subtitle mb-3 text-muted border-bottom pb-3">Posted on: <%= post.created_at %></h6>
    <%= post.body %>
  </div>
</div>